// Home — the default landing page. Intentionally empty: the CanvasBg
// carries the entire visual, and the Dock at the bottom is what the user
// reaches for first. Rendering `null` is load-bearing; don't put a
// greeting here. Winter's strength is that its home is a blank room, and
// we're copying that on purpose.
export function HomePage() {
  return null;
}
