{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "POC capabilities — bare minimum to run. Feature integrations land in subsequent phases.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
