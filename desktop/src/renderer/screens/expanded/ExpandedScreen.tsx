// Expanded chat window — Phase 4 placeholder. Built atop the conversation
// store (renderer/stores/conversation.ts, TBD) which subscribes to
// event:analyze-* for streaming deltas.

export function ExpandedScreen() {
  return (
    <div style={{ padding: 24, color: 'var(--d-text-2)' }}>
      Expanded chat — заглушка (Phase 4).
    </div>
  );
}
