// CustomLobbyPage был удалён в рамках Arena Wave-4 bugfix.
// Причина: UI на hardcoded списке комнат без реального backend-а;
// клики «войти» редиректили в несуществующий /hub, «создать» 401-ил
// несмотря на валидную сессию. Кастом-лобби — v2-фича, не MVP-критичная.
// См. финальный отчёт Wave-4 (item 10).
//
// Роут /lobby удалён в App.tsx. Этот файл оставлен как плейсхолдер-null
// на случай если где-то остался динамический import по пути.
export default function CustomLobbyPage() {
    return null;
}
