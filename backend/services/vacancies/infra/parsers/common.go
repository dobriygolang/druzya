// common.go — small helpers shared by every parser.
package parsers

// scraperUA is the polite User-Agent we send to every careers portal.
// Identifies the bot, links to a contact URL so admins can reach us
// before reaching for the rate-limit hammer.
const scraperUA = "Mozilla/5.0 (compatible; druz9-vacancies/1.0; +https://druz9.online)"
