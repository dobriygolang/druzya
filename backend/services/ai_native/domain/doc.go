// Package domain содержит сущности, value-объекты и интерфейсы репозиториев
// bounded-контекста ai_native (AI-Native Round, bible §19.1).
// Импорты внешних фреймворков сюда не допускаются.
//
// Инвариант безопасности — прочитать перед правкой:
//
//	solution_hint живёт ТОЛЬКО в TaskWithHint (потребляется BuildAssistantPrompt).
//	Любая клиентская форма (TaskPublic, DTO ProvenanceRecord, Scores, …)
//	НЕ ДОЛЖНА содержать это поле. Нарушение этого инварианта — баг утечки
//	информации.
//
// Домен строг к изоляции: мы НЕ импортируем druz9/ai_mock. Интерфейс
// LLMProvider передекларирован здесь, даже если по форме совпадает с
// ai_mock — каждый bounded-контекст владеет своими абстракциями.
package domain
