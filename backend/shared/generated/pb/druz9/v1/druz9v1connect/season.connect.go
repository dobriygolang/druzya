// season.proto — Connect-RPC contract for the `season` bounded context.
//
// Covers the single /season/current endpoint (GET). Season Pass progress is
// authenticated-only — the native Connect path is mounted behind requireAuth
// and REST traffic continues to flow through the gated group in main.go.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: druz9/v1/season.proto

package druz9v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	v1 "druz9/shared/generated/pb/druz9/v1"
	errors "errors"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SeasonServiceName is the fully-qualified name of the SeasonService service.
	SeasonServiceName = "druz9.v1.SeasonService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SeasonServiceGetCurrentProcedure is the fully-qualified name of the SeasonService's GetCurrent
	// RPC.
	SeasonServiceGetCurrentProcedure = "/druz9.v1.SeasonService/GetCurrent"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	seasonServiceServiceDescriptor          = v1.File_druz9_v1_season_proto.Services().ByName("SeasonService")
	seasonServiceGetCurrentMethodDescriptor = seasonServiceServiceDescriptor.Methods().ByName("GetCurrent")
)

// SeasonServiceClient is a client for the druz9.v1.SeasonService service.
type SeasonServiceClient interface {
	// GetCurrent returns the caller's Season Pass progress for the active
	// season (free + premium ladders, weekly challenges).
	GetCurrent(context.Context, *connect.Request[v1.GetCurrentSeasonRequest]) (*connect.Response[v1.SeasonProgress], error)
}

// NewSeasonServiceClient constructs a client for the druz9.v1.SeasonService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSeasonServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SeasonServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &seasonServiceClient{
		getCurrent: connect.NewClient[v1.GetCurrentSeasonRequest, v1.SeasonProgress](
			httpClient,
			baseURL+SeasonServiceGetCurrentProcedure,
			connect.WithSchema(seasonServiceGetCurrentMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// seasonServiceClient implements SeasonServiceClient.
type seasonServiceClient struct {
	getCurrent *connect.Client[v1.GetCurrentSeasonRequest, v1.SeasonProgress]
}

// GetCurrent calls druz9.v1.SeasonService.GetCurrent.
func (c *seasonServiceClient) GetCurrent(ctx context.Context, req *connect.Request[v1.GetCurrentSeasonRequest]) (*connect.Response[v1.SeasonProgress], error) {
	return c.getCurrent.CallUnary(ctx, req)
}

// SeasonServiceHandler is an implementation of the druz9.v1.SeasonService service.
type SeasonServiceHandler interface {
	// GetCurrent returns the caller's Season Pass progress for the active
	// season (free + premium ladders, weekly challenges).
	GetCurrent(context.Context, *connect.Request[v1.GetCurrentSeasonRequest]) (*connect.Response[v1.SeasonProgress], error)
}

// NewSeasonServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSeasonServiceHandler(svc SeasonServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	seasonServiceGetCurrentHandler := connect.NewUnaryHandler(
		SeasonServiceGetCurrentProcedure,
		svc.GetCurrent,
		connect.WithSchema(seasonServiceGetCurrentMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/druz9.v1.SeasonService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SeasonServiceGetCurrentProcedure:
			seasonServiceGetCurrentHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSeasonServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSeasonServiceHandler struct{}

func (UnimplementedSeasonServiceHandler) GetCurrent(context.Context, *connect.Request[v1.GetCurrentSeasonRequest]) (*connect.Response[v1.SeasonProgress], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("druz9.v1.SeasonService.GetCurrent is not implemented"))
}
